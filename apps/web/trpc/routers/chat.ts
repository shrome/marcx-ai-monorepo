// NOTE: This tRPC router is deprecated — the app now uses the REST backend via lib/backend/.
// Stubs kept to satisfy the AppRouter type without breaking imports.
import { router } from "../init"

export const chatRouter = router({})
