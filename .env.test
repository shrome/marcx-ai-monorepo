DATABASE_URL=postgresql://test:test@localhost:15433/marcx_test

# JWT (test-only secret — not used in production)
JWT_SECRET=test-jwt-secret-for-testing-only
JWT_REFRESH_SECRET=test-jwt-refresh-secret-for-testing-only
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Redis (not used in tests — token revocation is skipped)
REDIS_HOST=localhost
REDIS_PORT=6379

# AI-API (mocked in tests)
AI_API_URL=http://localhost:8000

# AWS S3 (mocked in tests)
AWS_S3_BUCKET=test-bucket
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Email (mocked in tests)
RESEND_API_KEY=test-resend-key

NODE_ENV=test
PORT=4001

# Fixed OTP for Playwright E2E tests (non-production only)
TEST_FIXED_OTP=000000
